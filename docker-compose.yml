version: '3.8'

services:
  # ============================================================================
  # 1. MongoDB Database
  # ============================================================================
  mongodb:
    image: mongo:6.0
    container_name: clinic_mongodb
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: secretpassword
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    networks:
      - clinic_net
      - dokploy-network

  # ============================================================================
  # 2. Mongo Express (قاعدة بيانات ويب تفاعلية لإدارة الجداول والمستندات)
  # ============================================================================
  mongo-express:
    image: mongo-express:latest
    container_name: clinic_mongo_express
    restart: always
    environment:
      ME_CONFIG_MONGODB_ADMINUSERNAME: root
      ME_CONFIG_MONGODB_ADMINPASSWORD: secretpassword
      ME_CONFIG_MONGODB_SERVER: mongodb
      ME_CONFIG_MONGODB_PORT: 27017
      ME_CONFIG_BASICAUTH_USERNAME: admin
      ME_CONFIG_BASICAUTH_PASSWORD: psypro2026
    ports:
      - "8081:8081"
    depends_on:
      - mongodb
    networks:
      - clinic_net
      - dokploy-network

  # ============================================================================
  # 3. Laravel Backend API
  # ============================================================================
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: clinic_backend
    restart: always
    environment:
      APP_NAME: PsyPro
      APP_ENV: production
      APP_KEY: base64:ZJ0PqR9O/UOwzp/vGnScZhSmRZmW/C88ursjb8Zvz+I=
      APP_DEBUG: "true"
      DB_CONNECTION: mongodb
      DB_HOST: mongodb
      DB_PORT: 27017
      DB_DATABASE: clinic_saas_db
      DB_USERNAME: root
      DB_PASSWORD: secretpassword
      AI_SERVICE_URL: http://ai-service:8000
    depends_on:
      - mongodb
    ports:
      - "8000:8000"
    networks:
      - clinic_net
      - dokploy-network

  # ============================================================================
  # 4. AI Clinical Service (FastAPI)
  # ============================================================================
  ai-service:
    build:
      context: ./ai-service
      dockerfile: Dockerfile
    container_name: clinic_ai_service
    restart: always
    environment:
      AI_PROVIDER: gemini
      GEMINI_API_KEY: ${GEMINI_API_KEY:-}
    ports:
      - "8001:8000"
    networks:
      - clinic_net
      - dokploy-network

  # ============================================================================
  # 5. Frontend Application (React + Vite SPA / Tablet)
  # ============================================================================
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: clinic_frontend
    restart: always
    depends_on:
      - backend
      - ai-service
    ports:
      - "3001:80"
    networks:
      - clinic_net
      - dokploy-network

networks:
  clinic_net:
    driver: bridge
  dokploy-network:
    external: true

volumes:
  mongo_data:
